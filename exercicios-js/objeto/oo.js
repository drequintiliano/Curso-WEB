// código não executavel

// procedural
processamento(valor1, valor2, valor3) //passando valores para um procedimento que vai manipula-los

// Orientação a Objetos
objeto = {
    valor1, //atributos
    valor2,
    valor3,
    processamento() { //comportamentos
        //...
    }
}

objeto.processamento() // foco passou a ser o objeto

// Principios importantes:
// 1.abstração
// 2.encapsulamento - interface simples para interagir com o objeto 
// 3.herança (prototype) - recebe atributos e comportamentos de um objeto pai
// 4.polimorfismo - multiplas formas,